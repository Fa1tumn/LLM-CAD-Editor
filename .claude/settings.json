{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r \".tool_response.filePath // .tool_input.file_path // empty\" | { read -r f; case \"$f\" in *.py) R=\"${CLAUDE_PROJECT_DIR:-.}/.venv/bin/ruff\"; [ -x \"$R\" ] || exit 0; \"$R\" format -q \"$f\"; \"$R\" check -q --fix --select I \"$f\"; \"$R\" check \"$f\";; esac; } 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "ruff format + lint"
          }
        ]
      }
    ]
  }
}
